import { Languages } from "@/components/help/topics/languages";

export const metadata = { title: "Swedish & English — Help" };

export default function Page() {
  return <Languages locale="en" />;
}
